@extends('public.layout')
@section('title','Reset Password | COU Youth')
@section('content')
<section style="max-width:520px;margin:40px auto"><div class="card"><h1 style="margin-top:0"><i class="fas fa-lock"></i> Reset Password</h1><p class="muted">Enter the six-digit code from your email and choose a new password.</p>@if(session('success'))<div class="flash">{{ session('success') }}</div>@endif @if($errors->any())<div style="padding:12px;background:#fef2f2;color:#991b1b;border-radius:9px;margin-bottom:14px">{{ $errors->first() }}</div>@endif<form method="post" action="{{ route('password.update') }}">@csrf<label>Email address<input class="field" type="email" name="email" value="{{ old('email',$email) }}" required></label><label>Reset code<input class="field" name="code" inputmode="numeric" maxlength="6" required></label><label>New password<input class="field" type="password" name="password" required><small class="muted">At least 8 characters with upper/lowercase letters and a number.</small></label><label>Confirm new password<input class="field" type="password" name="password_confirmation" required></label><button class="btn btn-primary" style="width:100%;justify-content:center"><i class="fas fa-lock-open"></i> Update Password</button></form></div></section>
<style>.field{display:block;width:100%;padding:11px;margin:6px 0 14px;border:1px solid #d1d5db;border-radius:9px}</style>
@endsection
