@extends('admin.layout') @section('title','Reports') @section('content')<h1>Reporting & Monitoring</h1><form method="get"><select name="organisation_unit_id"><option value="">All Church units</option>@foreach($units as $u)<option value="{{ $u->id }}" @selected(request('organisation_unit_id')==$u->id)>{{ $u->name }}</option>@endforeach</select><button>Filter</button></form><div class="grid" style="margin-top:16px">@foreach($summary as $label=>$value)<div class="card"><div>{{ ucwords(str_replace('_',' ',$label)) }}</div><h2>{{ number_format((float)$value) }}</h2></div>@endforeach</div>@endsection
