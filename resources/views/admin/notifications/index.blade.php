@extends('admin.layout') @section('title','Notifications') @section('content')<h1>Notifications</h1><div class="card"><form method="post" action="{{ route('admin.notifications.store') }}">@csrf<input name="title" placeholder="Title" required><textarea name="body" placeholder="Message" required></textarea><select name="channel"><option>in_app</option><option>push</option><option>email</option><option>all</option></select><select name="age_category"><option>all</option><option>teen</option><option>youth</option><option>young_adult</option></select><input name="organisation_unit_id" placeholder="Target unit ID (optional)"><button>Queue Notification</button></form></div><div class="card">@foreach($items as $n)<p><strong>{{ $n->title }}</strong> · {{ $n->channel }} · {{ $n->age_category }}</p>@endforeach{{ $items->links() }}</div>@endsection
