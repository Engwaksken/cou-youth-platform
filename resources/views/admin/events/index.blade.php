@extends('admin.layout') @section('title','Events') @section('content')<h1>Events</h1><div class="card"><form method="post" action="{{ route('admin.events.store') }}">@csrf<input name="title" placeholder="Event name" required><input name="starts_at" type="datetime-local" required><input name="venue" placeholder="Venue"><input name="organisation_unit_id" placeholder="Church unit ID"><input name="fee" type="number" step="0.01" value="0"><input name="currency" value="UGX" maxlength="3"><select name="status"><option>draft</option><option>published</option></select><button>Create Event</button></form></div><div class="card">@foreach($events as $e)<p><strong>{{ $e->title }}</strong> — {{ $e->starts_at }} — {{ $e->status }}</p>@endforeach{{ $events->links() }}</div>@endsection
