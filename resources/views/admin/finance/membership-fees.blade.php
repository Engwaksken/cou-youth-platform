@extends('admin.layout')
@section('title','Membership Fees')
@section('content')
<div class="page-head"><div><h1><i class="fas fa-coins"></i> Membership Fees</h1><p>Configure annual membership fees and record member payments.</p></div><div class="actions"><button class="btn btn-primary" data-open-modal="fee-add"><i class="fas fa-plus"></i> Add fee</button><button class="btn btn-light" data-open-modal="payment-add"><i class="fas fa-receipt"></i> Record payment</button></div></div>
<div class="grid stats-grid"><div class="card stat-card"><span class="stat-icon"><i class="fas fa-tags"></i></span><div><strong>{{ $stats['configured'] }}</strong><span>Configured fees</span></div></div><div class="card stat-card"><span class="stat-icon"><i class="fas fa-money-bill-wave"></i></span><div><strong>{{ number_format($stats['collected']) }}</strong><span>Collected</span></div></div><div class="card stat-card"><span class="stat-icon"><i class="fas fa-receipt"></i></span><div><strong>{{ $stats['payments'] }}</strong><span>Payments</span></div></div></div>
<div class="card"><form class="filters" method="GET"><input name="q" placeholder="Search fees" value="{{ request('q') }}"><input type="number" name="year" placeholder="Year" value="{{ request('year') }}"><button class="btn btn-primary">Filter</button><a class="btn btn-light" href="{{ route('admin.membership-fees.index') }}">Reset</a></form></div>
<div data-tabs><div class="tabs"><button class="tab active" type="button" data-tab-target="fees-list">Fees</button><button class="tab" type="button" data-tab-target="payments-list">Recent payments</button></div><section class="tab-panel active" id="fees-list"><div class="card table-card"><div class="table-wrap"><table><thead><tr><th>Name</th><th>Year</th><th>Amount</th><th>Church unit</th><th>Due</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($fees as $f)<tr><td><strong>{{ $f->name }}</strong><small>{{ Str::limit($f->description,80) }}</small></td><td>{{ $f->year }}</td><td>{{ $f->currency }} {{ number_format((float)$f->amount) }}</td><td>{{ $f->organisation_name ?: 'All units' }}</td><td>{{ $f->due_date ?: '—' }}</td><td><span class="badge {{ $f->is_active ? 'badge-success' : '' }}">{{ $f->is_active ? 'Active' : 'Inactive' }}</span></td><td class="actions"><button class="btn btn-light" data-open-modal="fee-edit-{{ $f->id }}">Edit</button><button class="btn btn-danger" data-open-modal="fee-delete-{{ $f->id }}">Delete</button></td></tr>@empty<tr><td colspan="7" class="empty">No membership fees configured.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $fees->links() }}</div></div></section><section class="tab-panel" id="payments-list"><div class="card table-card"><div class="table-wrap"><table><thead><tr><th>Date</th><th>Member</th><th>Fee</th><th>Amount</th><th>Method</th><th>Reference</th></tr></thead><tbody>@forelse($recentPayments as $p)<tr><td>{{ \Illuminate\Support\Carbon::parse($p->paid_at)->format('d M Y H:i') }}</td><td>{{ $p->member_name }}<small>{{ $p->member_reference }}</small></td><td>{{ $p->fee_name }}</td><td>{{ $p->currency }} {{ number_format((float)$p->amount) }}</td><td>{{ $p->payment_method ?: '—' }}</td><td>{{ $p->reference ?: '—' }}</td></tr>@empty<tr><td colspan="6" class="empty">No membership payments recorded.</td></tr>@endforelse</tbody></table></div></div></section></div>
<div class="modal" id="fee-add"><div class="modal-card"><div class="modal-head"><h2>Add Membership Fee</h2><button class="icon-btn" data-close-modal><i class="fas fa-xmark"></i></button></div><form method="POST" action="{{ route('admin.membership-fees.store') }}">@csrf @include('admin.finance._membership-fee-fields',['fee'=>null])<div class="modal-actions"><button type="button" class="btn btn-light" data-close-modal>Cancel</button><button class="btn btn-primary">Save</button></div></form></div></div>
<div class="modal" id="payment-add"><div class="modal-card"><div class="modal-head"><h2>Record Membership Payment</h2><button class="icon-btn" data-close-modal><i class="fas fa-xmark"></i></button></div><form method="POST" action="{{ route('admin.membership-fees.payments.store') }}">@csrf<div class="form-grid"><label>Fee<select name="membership_fee_id" required>@foreach($fees as $f)<option value="{{ $f->id }}">{{ $f->year }} — {{ $f->name }} ({{ $f->currency }} {{ number_format((float)$f->amount) }})</option>@endforeach</select></label><label>Member name<input name="member_name" required></label><label>Member reference<input name="member_reference"></label><label>Amount<input type="number" step="0.01" min="0.01" name="amount" required></label><label>Currency<input name="currency" value="UGX" maxlength="3" required></label><label>Payment method<select name="payment_method"><option>Cash</option><option>Mobile Money</option><option>Bank</option><option>Card</option><option>Other</option></select></label><label>Reference<input name="reference"></label><label>Paid at<input type="datetime-local" name="paid_at" value="{{ now()->format('Y-m-d\TH:i') }}" required></label></div><div class="modal-actions"><button type="button" class="btn btn-light" data-close-modal>Cancel</button><button class="btn btn-primary">Record payment</button></div></form></div></div>
@foreach($fees as $f)<div class="modal" id="fee-edit-{{ $f->id }}"><div class="modal-card"><div class="modal-head"><h2>Edit Membership Fee</h2><button class="icon-btn" data-close-modal><i class="fas fa-xmark"></i></button></div><form method="POST" action="{{ route('admin.membership-fees.update',$f->id) }}">@csrf @method('PUT') @include('admin.finance._membership-fee-fields',['fee'=>$f])<div class="modal-actions"><button type="button" class="btn btn-light" data-close-modal>Cancel</button><button class="btn btn-primary">Update</button></div></form></div></div><div class="modal" id="fee-delete-{{ $f->id }}"><div class="modal-card small"><div class="modal-head"><h2>Delete membership fee?</h2><button class="icon-btn" data-close-modal><i class="fas fa-xmark"></i></button></div><p>Deleting <strong>{{ $f->name }}</strong> will also remove its recorded payments.</p><form method="POST" action="{{ route('admin.membership-fees.destroy',$f->id) }}">@csrf @method('DELETE')<div class="modal-actions"><button type="button" class="btn btn-light" data-close-modal>Cancel</button><button class="btn btn-danger">Delete</button></div></form></div></div>@endforeach
@endsection
