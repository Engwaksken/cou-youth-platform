@extends('admin.layout') @section('title','Guardian Consents') @section('content')<h1>Guardian Consents</h1><div class="card">@foreach($consents as $c)<div style="padding:12px 0;border-bottom:1px solid #eee"><strong>{{ $c->guardian_name }}</strong> — {{ $c->status }} @if($c->status==='pending')<form style="display:inline" method="post" action="{{ route('admin.guardian-consents.approve',$c) }}">@csrf<button>Approve</button></form><form style="display:inline" method="post" action="{{ route('admin.guardian-consents.reject',$c) }}">@csrf<button>Reject</button></form>@endif</div>@endforeach{{ $consents->links() }}</div>@endsection
