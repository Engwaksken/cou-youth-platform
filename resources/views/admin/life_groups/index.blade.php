@extends('admin.layout') @section('title','Life Groups') @section('content')<h1>Life Groups</h1><div class="card"><form method="post" action="{{ route('admin.life-groups.store') }}">@csrf<input name="name" placeholder="Group name" required><input name="organisation_unit_id" placeholder="Church unit ID"><input name="leader_user_id" placeholder="Leader user ID"><input name="member_limit" type="number" value="12" min="2" max="12"><input name="meeting_day" placeholder="Meeting day"><input name="meeting_time" type="time"><button>Create Life Group</button></form></div><div class="card">@foreach($groups as $g)<p><strong>{{ $g->name }}</strong> — {{ $g->members_count }}/{{ $g->member_limit }} members</p>@endforeach{{ $groups->links() }}</div>@endsection
