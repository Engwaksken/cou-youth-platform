@extends('admin.layout')
@section('content')<h1>Church Locator</h1><form method="post" action="{{route('admin.church-locations.store')}}">@csrf<select name="organisation_unit_id" required>@foreach($units as $u)<option value="{{$u->id}}">{{$u->name}}</option>@endforeach</select><input name="name" required placeholder="Church/location name"><input name="address" placeholder="Address"><input name="latitude" placeholder="Latitude"><input name="longitude" placeholder="Longitude"><input name="service_times" placeholder="Service times"><input name="youth_fellowship_times" placeholder="Youth fellowship times"><input name="phone" placeholder="Phone"><input name="email" placeholder="Email"><button>Add</button></form>
@foreach($items as $i)<article><strong>{{$i->name}}</strong> — {{$i->organisationUnit?->name}} — {{$i->address}} <form method="post" action="{{route('admin.church-locations.destroy',$i)}}">@csrf @method('DELETE')<button>Delete</button></form></article>@endforeach {{$items->links()}}@endsection
