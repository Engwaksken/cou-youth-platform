@extends('admin.layout')
@section('content')<h1>Payment Gateways</h1><p>Credentials are encrypted at rest and never returned by public APIs.</p><form method="post" action="{{route('admin.payment-gateways.store')}}">@csrf<input name="name" required placeholder="Gateway name"><input name="slug" placeholder="Slug"><input name="provider" required placeholder="Provider"><input name="currency" value="UGX" maxlength="3" required><input name="api_key" placeholder="API/Public key"><input type="password" name="api_secret" placeholder="API secret"><input type="password" name="webhook_secret" placeholder="Webhook secret"><input name="callback_url" placeholder="Callback URL"><label><input type="checkbox" name="is_test_mode" value="1"> Test mode</label><label><input type="checkbox" name="is_enabled" value="1"> Enabled</label><button>Add gateway</button></form>
@foreach($items as $g)<article><strong>{{$g->name}}</strong> — {{$g->provider}} — {{$g->currency}} — {{ $g->is_enabled?'Enabled':'Disabled' }} <form method="post" action="{{route('admin.payment-gateways.destroy',$g)}}">@csrf @method('DELETE')<button>Delete</button></form></article>@endforeach@endsection
