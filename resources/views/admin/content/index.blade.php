@extends('admin.layout') @section('title','Content') @section('content')<h1>Content</h1><div class="card"><form method="post" action="{{ route('admin.content.store') }}">@csrf <input name="title" placeholder="Title" required><select name="type">@foreach(['news','announcement','devotion','bible_study','resource','opportunity'] as $t)<option>{{ $t }}</option>@endforeach</select><select name="status"><option>draft</option><option>pending</option><option>published</option></select><textarea name="body" placeholder="Content" required></textarea><button>Save</button></form></div><div class="card">@foreach($items as $item)<p><strong>{{ $item->title }}</strong> · {{ $item->type }} · {{ $item->status }}</p>@endforeach{{ $items->links() }}</div>@endsection
